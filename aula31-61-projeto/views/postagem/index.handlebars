<div class="card mt-4">
    <div class="card-body">
        <h1>{{postagem.title}}</h1>
        <hr>
        <small>Data de publicação: {{postagem.date}}</small>
        <hr>
        {{postagem.content}}
    </div>
</div>
